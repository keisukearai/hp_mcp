{
  "name": "@kotoragk/hp-mcp",
  "version": "0.1.0",
  "description": "Sample MCP server that exposes the kotoragk HP (homepage) public API as read-only tools.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "hp-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "claude",
    "sample"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
